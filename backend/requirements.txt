# ===== サーバー本体（dummy 実装ならこれだけで動く）=====
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pyngrok==7.2.0
requests==2.32.3

# ===== AI バックエンド（Qwen3-TTS / Whisper）=====
# 既定は Qwen3-TTS + Whisper。重い依存は backend/requirements-ai.txt に分離している。
# Colab では colab_runner が（dummy 以外のとき）自動で requirements-ai.txt も入れる。
#   pip install -r backend/requirements-ai.txt
